package com.lovettj.surfspotsapi.enums;

/**
 * Surf session log only (wave height bucket). Stored as enum name in DB.
 */
public enum WaveSize {
    SMALL,
    CHEST_SHOULDER,
    HEAD_PLUS,
    GIANT
}
